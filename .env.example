# Copy this file to ".env" and fill in your real values.
# Vite only exposes variables prefixed with VITE_ to the browser.

# Frontend (Vite) - public (safe to share)
VITE_UNSPLASH_ACCESS_KEY=your_unsplash_key

# Backend (Node/Encore runtime) - private (do NOT expose to frontend)
UNSPLASH_ACCESS_KEY=your_unsplash_key

# Polar.sh (Server-side only, enables the Store feature)
# Get these from https://polar.sh
POLAR_API_KEY=your_polar_api_key
POLAR_ORGANIZATION_ID=org_XXXXXXXXXXXX
